[www]
; Listen on port 9000 inside the adminer container
listen = 0.0.0.0:9000

; Use the same UID as your FTP and WordPress for permission harmony
user = 1000
group = 1000

; Process management
pm = dynamic
pm.max_children = 5
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3
